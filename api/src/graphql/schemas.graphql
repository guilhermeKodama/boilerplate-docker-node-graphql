scalar Date

type User {
  id: ID!,
  email: String!,
  role: String,
  createdAt: Date,
  token: String
}

type Query {
  me: User,
  users: [User],
  user(id: ID!): User
}

type Mutation {
  login(email: String!, password: String!): User
}
